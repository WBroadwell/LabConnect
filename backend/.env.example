# Database connection string
SQLALCHEMY_DATABASE_URI=postgresql://user:password@localhost:5432/labconnect
#SQLALCHEMY_DATABASE_URI=postgresql://postgres:postgres@localhost:5432/labconnect
#SQLALCHEMY_DATABASE_URI=postgresql://postgres:postgres@localhost:5432/labconnectdb
#SQLALCHEMY_DATABASE_URI="postgresql://postgres:postgres@localhost:5432/labconnectdb"
#app.config["SQLALCHEMY_DATABASE_URI"] = "postgresql://postgres:postgres@127.0.0.1:5432/labconnectdb"

#import os
#print("DB URI =", os.getenv("SQLALCHEMY_DATABASE_URI")

#print("DB URI:", app.config["SQLALCHEMY_DATABASE_URI"])


# Flask environment: development, production, or testing
FLASK_ENV=development

# Secret key for Flask session signing
SECRET_KEY=your-secret-key-here

# Secret key for JWT token signing
JWT_SECRET_KEY=your-jwt-secret-key-here

# Frontend URL (used for CORS and auth redirects)
FRONTEND_URL=http://localhost:3000

# Path to SAML configuration directory (optional, has default)
# SAML_CONFIG=/path/to/saml/config
