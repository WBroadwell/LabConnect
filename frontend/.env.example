# URL of the Flask backend server (used by Next.js rewrites proxy)
BACKEND_URL=http://localhost:5000

# App environment — mirrors FLASK_ENV. Set to "production" to enable SSO/Shibboleth login.
# Any other value (development, testing) enables the auto-admin bypass.
NEXT_PUBLIC_APP_ENV=development
